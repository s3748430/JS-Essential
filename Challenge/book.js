class Books {
  constructor(
    name,
    author,
    gender,
    publishDate,
    Language,
    rate,
    dateAccquired
  ) {
    this.name = name;
    this.author = author;
    this.gender = gender;
    this.publishDate = publishDate;
    this.Language = Language;
    this.rate = rate;
    this.dateAccquired = dateAccquired;
  }
}
